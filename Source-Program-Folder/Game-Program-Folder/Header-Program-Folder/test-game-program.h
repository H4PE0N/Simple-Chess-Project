
#ifndef TEST_GAME_PROGRAM_H
#define TEST_GAME_PROGRAM_H

// bool test_game_loop(Board, Info*, SDL_Renderer*, SDL_Window*);

bool computer_move_handler(Board, Info*);

bool user_move_handler(Board, Info*);

#endif
